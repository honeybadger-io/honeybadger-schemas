{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "insights/v1/events/honeybadger-laravel/response.received.json",
  "title": "Laravel: response.received",
  "description": "An outbound HTTP response received via Laravel's HTTP client. Emitted by ResponseReceived.",
  "type": "object",
  "properties": {
    "event_type": {
      "type": "string",
      "const": "response.received"
    },
    "uri": {
      "type": "string",
      "description": "URL of the outbound request."
    },
    "statusCode": {
      "type": "integer",
      "description": "HTTP response status code."
    },
    "duration": {
      "type": "number",
      "description": "Duration in microseconds. The client emits a string with ms suffix (e.g. \"5.123ms\"); the ingestion pipeline converts it to microseconds."
    },
    "requestId": {
      "type": "string",
      "description": "Request ID from the X-Request-Id or Request-Id header, or a generated UUID. Set by AssignRequestId middleware into Laravel's shared log context."
    },
    "request_id": {
      "type": "string",
      "description": "Alias \u2014 same value as requestId, present if set via event context."
    }
  },
  "required": [
    "event_type",
    "uri",
    "statusCode"
  ]
}
