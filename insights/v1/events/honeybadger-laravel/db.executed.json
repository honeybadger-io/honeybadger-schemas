{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "insights/v1/events/honeybadger-laravel/db.executed.json",
  "title": "Laravel: db.executed",
  "description": "A database query executed via Laravel's Eloquent/Query Builder. SQL is sanitized \u2014 literals replaced with ?.",
  "type": "object",
  "properties": {
    "event_type": {
      "type": "string",
      "const": "db.executed"
    },
    "connectionName": {
      "type": "string",
      "description": "Database connection name, e.g. \"mysql\", \"pgsql\"."
    },
    "sql": {
      "type": "string",
      "description": "Sanitized SQL with literals replaced by ?."
    },
    "duration": {
      "type": "number",
      "description": "Duration in microseconds. The client emits a string with ms suffix (e.g. \"5.123ms\"); the ingestion pipeline converts it to microseconds."
    },
    "requestId": {
      "type": "string",
      "description": "Request ID from the X-Request-Id or Request-Id header, or a generated UUID. Set by AssignRequestId middleware into Laravel's shared log context."
    },
    "request_id": {
      "type": "string",
      "description": "Alias \u2014 same value as requestId, present if set via event context."
    }
  },
  "required": [
    "event_type",
    "connectionName",
    "sql"
  ]
}
