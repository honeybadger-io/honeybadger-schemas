{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "insights/v1/events/honeybadger-laravel/job.queued.json",
  "title": "Laravel: job.queued",
  "description": "A job was pushed onto a queue. Emitted by JobQueued. Requires Laravel 8.24+.",
  "type": "object",
  "properties": {
    "event_type": {
      "type": "string",
      "const": "job.queued"
    },
    "connectionName": {
      "type": "string",
      "description": "Queue connection name."
    },
    "job": {
      "type": "string",
      "description": "Job class name."
    },
    "id": {
      "type": "string",
      "description": "Job ID."
    },
    "queue": {
      "type": [
        "string",
        "null"
      ],
      "description": "Queue name, if available."
    },
    "delay": {
      "type": [
        "integer",
        "null"
      ],
      "description": "Delay in seconds before the job becomes available."
    },
    "requestId": {
      "type": "string",
      "description": "Request ID from the X-Request-Id or Request-Id header, or a generated UUID. Set by AssignRequestId middleware into Laravel's shared log context."
    },
    "request_id": {
      "type": "string",
      "description": "Alias \u2014 same value as requestId, present if set via event context."
    }
  },
  "required": [
    "event_type",
    "connectionName",
    "job",
    "id"
  ]
}
