{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "insights/v1/events/honeybadger-ruby/halted_callback.action_controller.json",
  "title": "ActionController: halted_callback",
  "description": "A before/around filter halted the Rails request processing chain.",
  "type": "object",
  "properties": {
    "event_type": {
      "type": "string",
      "const": "halted_callback.action_controller"
    },
    "filter": {
      "type": "string",
      "description": "Name of the filter/callback that halted the chain."
    },
    "duration": {
      "type": "number",
      "description": "Duration in milliseconds."
    },
    "instrumenter_id": {
      "type": "string",
      "description": "Rails instrumenter UUID."
    },
    "request_id": {
      "type": "string",
      "description": "Rails request UUID, present on any event fired during a web request context. Merged globally by the Honeybadger agent."
    }
  },
  "required": [
    "event_type",
    "filter"
  ]
}
